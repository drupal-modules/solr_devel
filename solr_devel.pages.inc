<?php

/**
 * @file
 * Page callbacks for the Solr Devel module.
 */

function solr_devel_node_page($node) {

  // Define an environment hook and iterate over all connections.
  // $adapter->entityIndexed();

  return array();
}